% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{H5ClusterRun}
\alias{H5ClusterRun}
\title{Constructor for H5ClusterRun Objects}
\usage{
H5ClusterRun(file, scan_name, mask, clusters, n_time = NULL, compress = FALSE)
}
\arguments{
\item{file}{Character string path to the HDF5 file.}

\item{scan_name}{The character string name of the scan (e.g., "run1").}

\item{mask}{A \code{LogicalNeuroVol} object for the brain mask.}

\item{clusters}{A \code{ClusteredNeuroVol} object for cluster assignments.}

\item{n_time}{(Optional) The number of time points. If \code{NULL} (default), the function
will attempt to read it from the HDF5 file attributes or metadata dataset.}

\item{compress}{(Optional) Logical indicating compression status (metadata).}
}
\value{
A new \code{H5ClusterRun} object with an open file handle managed by the object.
}
\description{
Creates a new \code{H5ClusterRun} object, representing a single run of full
voxel-level clustered data from an HDF5 file.

This function handles file opening/closing and reads necessary metadata like
\code{n_time} from the HDF5 file if not provided explicitly.
}
\examples{
\donttest{
# Create temporary HDF5 file with minimal experiment structure
temp_file <- tempfile(fileext = ".h5")
exp_file <- fmristore:::create_minimal_h5_for_H5ClusterExperiment(file_path = temp_file)

# Create mask and clusters
mask <- fmristore:::create_minimal_LogicalNeuroVol(dims = c(5, 5, 4))
clusters <- fmristore:::create_minimal_ClusteredNeuroVol(mask_vol = mask, num_clusters = 3)

# Create H5ClusterRun object
run <- H5ClusterRun(exp_file, scan_name = "Run1_Full", mask = mask, clusters = clusters)

# Access properties
print(run@n_time)
print(dim(mask))

# Clean up
close(run)
unlink(temp_file)
}

}
