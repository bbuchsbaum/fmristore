% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/array_source.R
\name{h5_read_plan}
\alias{h5_read_plan}
\alias{validate_h5_read_amplification}
\title{Plan and enforce HDF5 read amplification}
\usage{
h5_read_plan(x, observations = NULL, features = NULL)

validate_h5_read_amplification(plan, max = 1.5)
}
\arguments{
\item{x}{An `h5_array_source` descriptor.}

\item{observations, features}{Logical selectors accepted by
[fmridataset::source_read()]. Duplicate positions do not cause additional
physical reads.}

\item{plan}{A plan returned by `h5_read_plan()`.}

\item{max}{Maximum permitted ratio of physically touched values to unique
requested values.}
}
\value{
`h5_read_plan()` returns a serializable `h5_read_plan` list;
  `validate_h5_read_amplification()` returns `plan` invisibly or errors.
}
\description{
`h5_read_plan()` estimates the physical HDF5 chunks touched by a logical
observation-by-feature selection. It uses descriptor metadata only and does
not open or read the dataset. `validate_h5_read_amplification()` turns the
estimate into an executable performance gate.
}
