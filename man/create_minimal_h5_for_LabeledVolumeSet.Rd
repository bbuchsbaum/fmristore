% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz_example_helpers.R
\name{create_minimal_h5_for_LabeledVolumeSet}
\alias{create_minimal_h5_for_LabeledVolumeSet}
\title{Create a minimal HDF5 file suitable for LabeledVolumeSet examples (via read_labeled_vec)}
\usage{
create_minimal_h5_for_LabeledVolumeSet(
  vol_dims = c(4L, 4L, 3L),
  labels = c("Set1", "Set2"),
  num_vols_per_label = 1L,
  file_path = NULL
)
}
\arguments{
\item{vol_dims}{3D spatial dimensions for each volume, e.g., c(4L, 4L, 3L).}

\item{labels}{A character vector of labels, e.g., c("ConditionA", "ConditionB").}

\item{num_vols_per_label}{Integer, number of volumes to generate for each label.}

\item{file_path}{Optional: path to HDF5 file. If \code{NULL}, a temp file is created.}
}
\value{
Path to the created HDF5 file.
}
\description{
This function creates a temporary HDF5 file with a minimal structure 
that can be read by \code{read_labeled_vec} to produce a \code{LabeledVolumeSet}.
}
\details{
Refer to \code{write_labeled_vec} and \code{read_labeled_vec} for the expected structure.
}
\examples{
# Not typically called directly by users, used in other examples via fmristore:::
if (requireNamespace("neuroim2", quietly = TRUE) && 
    requireNamespace("hdf5r", quietly = TRUE) && 
    exists("read_labeled_vec", where = "package:fmristore")) {
  temp_file <- fmristore:::create_minimal_h5_for_LabeledVolumeSet()
  print(temp_file)
  # Example usage (now should work with read_labeled_vec):
  lvs <- NULL
  tryCatch({
    lvs <- fmristore::read_labeled_vec(temp_file)
    print(lvs)
    print(labels(lvs))
  }, finally = {
    if (!is.null(lvs)) try(close(lvs), silent = TRUE)
    if (file.exists(temp_file)) unlink(temp_file)
  })
}
}
\keyword{internal}
